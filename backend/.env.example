# =============================================================================
# ClearSide Backend Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Configuration (Required)
# Choose one provider and set its API key
# -----------------------------------------------------------------------------
LLM_PROVIDER=openai
# Options: openai, anthropic

# OpenAI Configuration
OPENAI_API_KEY=sk-...
LLM_DEFAULT_MODEL=gpt-4

# Anthropic Configuration (alternative)
# ANTHROPIC_API_KEY=sk-ant-...
# LLM_DEFAULT_MODEL=claude-3-sonnet-20240229

# -----------------------------------------------------------------------------
# TTS Configuration (Optional - for audio/podcast export)
# -----------------------------------------------------------------------------
# ElevenLabs (premium quality)
# ELEVENLABS_API_KEY=sk_...

# Google AI Studio - Gemini 2.5 TTS (premium quality, multi-speaker support)
# GOOGLE_AI_API_KEY=AIza...

# Google Cloud Long Audio Synthesis (async, up to 1MB input, no chunking needed)
# Requires service account JSON with TTS and GCS permissions
# GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}
# GCS bucket for audio output (must exist, service account needs write access)
# GOOGLE_CLOUD_TTS_BUCKET=your-bucket-name
# Optional: Project ID (usually extracted from service account JSON)
# GOOGLE_CLOUD_PROJECT_ID=your-project-id

# OpenRouter (for Perplexity research)
# OPENROUTER_API_KEY=sk-or-v1-...

LLM_TIMEOUT_MS=30000
LLM_MAX_RETRIES=3

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

# Option 1: Individual config (local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clearside
DB_USER=postgres
DB_PASSWORD=your_secure_password

# Option 2: Connection URL (Supabase/Neon/Railway - production)
# When DATABASE_URL is set, it takes precedence over individual config
# DATABASE_URL=postgresql://user:password@host:5432/clearside

# Supabase example:
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3001
NODE_ENV=development

# CORS - Frontend URL (required for production)
# Set to your Vercel frontend URL
# FRONTEND_URL=https://clearside.vercel.app

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log levels: fatal, error, warn, info, debug, trace
# Development: use 'debug' for detailed logs
# Production: use 'info' or 'warn'
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Error Tracking (Optional)
# -----------------------------------------------------------------------------
# Sentry DSN for production error monitoring
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# SENTRY_SAMPLE_RATE=0.1
