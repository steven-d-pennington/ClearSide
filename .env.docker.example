# ClearSide Docker Environment Configuration
# Copy this file to .env.docker and fill in your API keys
#
# Usage: cp .env.docker.example .env.docker

# =============================================================================
# LLM Configuration (Required - choose one provider)
# =============================================================================

# Option 1: OpenAI (default)
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-your-openai-api-key-here
LLM_DEFAULT_MODEL=gpt-4

# Option 2: Anthropic (uncomment to use instead)
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here
# LLM_DEFAULT_MODEL=claude-3-sonnet-20240229

# LLM Settings
LLM_TIMEOUT_MS=30000
LLM_MAX_RETRIES=3

# =============================================================================
# Logging
# =============================================================================
LOG_LEVEL=debug

# =============================================================================
# Database (automatically configured by docker-compose, no changes needed)
# =============================================================================
# DATABASE_URL is set in docker-compose.yml

# =============================================================================
# Frontend (automatically configured by docker-compose, no changes needed)
# =============================================================================
# VITE_API_URL is set in docker-compose.yml
